import { SimpleStyleDirectiveDirective } from './simple-style-directive.directive';

describe('SimpleStyleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleStyleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
